import type {DockviewTheme} from "dockview-core";

export const evergreen: DockviewTheme = {
    name: "Evergreen",
    className: "dockview-theme-evergreen",
    gap: 10,
    dndOverlayMounting: "absolute",
    dndPanelOverlay: "group"
}